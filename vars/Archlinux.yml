---
__redis_package: redis
redis_conf_path: /etc/{{ redis_daemon }}.conf

# Service vars
redis_svc_path: /usr/lib/systemd/system/
redis_svc_file: "{{ redis_daemon }}.service"

read_write_dirs:
  - "{{ redis_pidfile | dirname }}"
  - "{{ redis_logfile | dirname }}"
  - "{{ redis_dbdir }}"

